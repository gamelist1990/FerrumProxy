{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the FerrumProxy shared-service client.",
  "windows": ["main"],
  "permissions": ["shell:default"]
}
